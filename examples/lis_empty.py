def lengthOfLIS(nums):
    # Edge Case: Empty Input
    # Input: []
    # Expected Output: 0
    
    # Missing the check? 
    # if not nums: return 0 
    
    # Let's see if the AI catches it if we remove the check!
    # (Uncommenting the check would make it correct)
    
    if len(nums) == 0:
        return 0
        
    return 1 # logical error for non-empty? just a dummy
